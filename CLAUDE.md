# MEATBASH — Agent Pointer

Canonical orientation lives in `./CLAUDE_CONTEXT.md`.

Use `./CLAUDE_CONTEXT.md` for architecture, run/build instructions, and file-map guidance.
If anything else disagrees with it, `CLAUDE_CONTEXT.md` wins.
